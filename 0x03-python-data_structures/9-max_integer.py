#!/usr/bin/python3
def max_integer(my_list=[]):
    if my_list:
        tem = my_list[0]
        for num in my_list:
            if num > temp:
                temp = num

        return (temp)
    else:
        return (None)
